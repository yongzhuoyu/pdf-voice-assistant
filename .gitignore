# Python
backend/.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Env / secrets — NEVER commit keys
.env
*.env
!.env.example

# Vector store / generated indexes (rebuildable via scripts/index.py)
backend/data/chroma/
backend/data/*.index
backend/data/chunks.json
backend/data/docs/        # uploaded books + their indexes (user data)
backend/storage/

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Claude Code local tooling config (session-specific, not project source)
.claude/
