# Python
backend/.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Env / secrets — NEVER commit keys
.env
*.env
!.env.example

# Vector store / generated indexes (rebuilt automatically when a book is uploaded)
backend/data/chroma/
backend/data/*.index
# uploaded books + their indexes (user data)
backend/data/docs/
backend/storage/

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Claude Code local tooling config (session-specific, not project source)
.claude/
