# --- Core RAG pipeline (Day 1) ---
anthropic>=0.69          # Claude API: contextualizer + grounded generation w/ native citations
chromadb>=0.5            # local vector store (embeddings + dense search)
rank-bm25>=0.2.2         # BM25 lexical search (the hybrid-search other half)
sentence-transformers>=3 # local cross-encoder reranker (no API cost per query)

# --- PDF parsing ---
pypdf>=5                 # text + per-page extraction (already used to build the test PDF)
pdfplumber>=0.11         # per-character font sizes, for font-based chapter detection

# --- Config / plumbing ---
python-dotenv>=1         # load ANTHROPIC_API_KEY (and later DEEPGRAM_API_KEY) from .env

# --- API + voice (Day 2) ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9  # FastAPI file uploads (audio in)
httpx>=0.27              # Deepgram REST calls (ASR/TTS); also an anthropic dep

# --- Testing (a graded deliverable) ---
pytest>=8
